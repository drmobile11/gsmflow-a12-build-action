# Nuitka Anti-Bloat Configuration
# Prevents unwanted module imports that slow down compilation

[anti-bloat]
# Ignore IPython imports from pymobiledevice3 (not needed for core functionality)
pymobiledevice3.services.remote_server: ignore IPython
